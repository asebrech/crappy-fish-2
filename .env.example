# Game Server URL (WebSocket)
# For local development, use: ws://localhost:2567
# For production, use your Render URL: wss://your-app-name.onrender.com
NEXT_PUBLIC_GAME_SERVER_URL=ws://localhost:2567

# Server Configuration
# Debug mode: Set to "true" to start game with 1 player (for testing)
# Set to "false" for production (requires 2+ players)
DEBUG_MODE=false
NODE_ENV=development
